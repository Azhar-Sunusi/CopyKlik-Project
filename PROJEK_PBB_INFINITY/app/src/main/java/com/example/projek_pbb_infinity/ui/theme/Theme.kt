package com.example.projek_pbb_infinity.ui.theme

import androidx.compose.material3.lightColorScheme

// Di dalam file Theme.kt
private val LightColorScheme = lightColorScheme(
    primary = BlueButton,
    secondary = BlueButton,
    background = PeachBackground, // Terapkan di sini
    onBackground = DeepBlackText,
    surface = PeachBackground,
    onSurface = DeepBlackText,
    onPrimary = WhiteText
)